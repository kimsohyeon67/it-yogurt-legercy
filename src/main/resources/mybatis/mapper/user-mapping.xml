<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
        
<mapper namespace="com.starters.ityogurt.dao.UserDAO">

<!-- <select id="getAllUserlist" parameterType="">
	select * 
	from user
</select>

<select id="getUserByUserSeq" parameterType="">
	select * 
	from user
	where user_seq = #{userSeq}
</select>

<select id="declarationUser" parameterType="">
	select * 
	from user
	where declaration >= #{declarationNum}
</select> -->

</mapper>